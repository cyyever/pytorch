// Generated by CMake. Records the build settings that
// torch.__config__.show() reports.

#pragma once

#define TORCH_BUILD_STRINGS { \
  {"TORCH_VERSION", "${TORCH_VERSION}"}, \
  {"CXX_COMPILER", R"PYT(${CMAKE_CXX_COMPILER})PYT"}, \
  {"CXX_FLAGS", R"PYT(${CMAKE_CXX_FLAGS})PYT"}, \
  {"CUDA_FLAGS", R"PYT(${CMAKE_CUDA_FLAGS})PYT"}, \
  {"BUILD_TYPE", "${CMAKE_BUILD_TYPE}"}, \
  {"BLAS_INFO", "${BLAS_INFO}"}, \
  {"LAPACK_INFO", "${LAPACK_INFO}"}, \
  {"USE_CUDA", "${USE_CUDA}"}, \
  {"USE_ROCM", "${USE_ROCM}"}, \
  {"CUDA_VERSION", "${CUDA_VERSION}"}, \
  {"ROCM_VERSION", "${ROCM_VERSION}"}, \
  {"USE_CUDNN", "${USE_CUDNN}"}, \
  {"COMMIT_SHA", "${COMMIT_SHA}"}, \
  {"CUDNN_VERSION", "${CUDNN_VERSION}"}, \
  {"USE_NCCL", "${USE_NCCL}"}, \
  {"USE_OPENMP", "${USE_OPENMP}"}, \
  {"USE_MKL", "${CAFFE2_USE_MKL}"}, \
  {"USE_ROCM_KERNEL_ASSERT", "${USE_ROCM_KERNEL_ASSERT}"}, \
  {"USE_CUSPARSELT", "${USE_CUSPARSELT}"}, \
  {"USE_HIPSPARSELT", "${USE_HIPSPARSELT}"}, \
  {"USE_XPU", "${USE_XPU}"}, \
  {"USE_XCCL", "${USE_XCCL}"}, \
  {"SYCL_COMPILER_VERSION", "${SYCL_COMPILER_VERSION}"}, \
}
